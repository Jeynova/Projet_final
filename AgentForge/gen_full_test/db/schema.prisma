// Prisma schema file
model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String?
  password  String
  tasks     Task[]
}

type Post {
  id    Int      @id @default(autoincrement())
  title String
  body  String
  userId Int
}